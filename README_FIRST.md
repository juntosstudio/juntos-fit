# Start Check-In Focus + Glow Fix

Extract this ZIP into:

`C:\FitnessCoach\App`

Allow Windows to merge folders and replace all three app files.

This patch keeps unanswered fields orange before focus on web and mobile.
It also:

- focuses the active measurement field automatically
- selects a saved value so it can be replaced quickly
- sends **Edit Value** directly back to the warned field
- selects the warned value for immediate editing

No database migration is needed.

Test locally, then run your normal build/push command.

Juntos Fit — Cardio Effort Layout v3

This fixes the 2-over-1 layout at the source instead of fighting the shared CSS.

Effort no longer uses the generic WizardChoiceGroup/.choice-list. It has a dedicated
three-column radio group, so Easy | Moderate | Hard always remain on one row.

UI only. No DB push. No Edge deploy. Run tests normally after extracting.
